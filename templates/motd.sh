#!/bin/bash
# VexDeploy MOTD template reference — variables substituted at runtime.
# Prefer /brand-template; this file documents available {vars}.

# {brand_name} {tagline} {footer}
# {website} {discord} {support}
# {hostname} {os} {kernel} {uptime}
# {cpu} {ram_used} {ram_total} {ram_percent} {disk} {ip} {users} {processes}
